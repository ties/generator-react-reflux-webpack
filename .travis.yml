language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-refluxjs-webpack' ]; then cd .. && eval "mv $currentfolder generator-react-reactjs-webpack" && cd generator-react-refluxjs-webpack; fi
