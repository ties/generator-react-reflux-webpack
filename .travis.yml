language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-react-reflux-webpack' ]; then cd .. && eval "mv $currentfolder generator-react-react-webpack" && cd generator-react-reflux-webpack; fi
